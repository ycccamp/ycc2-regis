import { theme as defaultTheme } from '@chakra-ui/core'
export const theme = {
  ...defaultTheme,
  fonts: {
    ...defaultTheme.fonts,
    heading: `'Poppins', Futura, Nunito, Prompt, -apple-system, 'Helvetica Neue', Helvetica, Arial, system-ui, sans-serif`,
    body: `'Sarabun', -apple-system, 'Helvetica Neue', Helvetica, Arial, system-ui, sans-serif`,
  },
}
